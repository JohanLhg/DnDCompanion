<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DnDCompanionApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_dnd"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_round_dnd"
        android:supportsRtl="true"
        android:theme="@style/Theme.DnDCompanion"
        tools:targetApi="31">
        <activity
            android:name=".feature_character_selection.presentation.CharacterSelectionActivity"
            android:exported="false"
            android:label="@string/title_activity_character_selection"
            android:theme="@style/Theme.DnDCompanion" />
        <activity
            android:name=".feature_loading_data.LoadingActivity"
            android:exported="false"
            android:label="@string/title_activity_loading"
            android:theme="@style/Theme.DnDCompanion" />
        <activity
            android:name=".feature_authentication.presentation.AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.DnDCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>